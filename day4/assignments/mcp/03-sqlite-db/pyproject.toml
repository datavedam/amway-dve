[project]
name = "assignment-03-sqlite-db"
version = "0.1.0"
description = "Assignment 3: MCP tools backed by a SQLite database"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=2.2.0",
]

[tool.uv]
package = false
