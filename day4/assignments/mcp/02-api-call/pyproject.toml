[project]
name = "assignment-02-api-call"
version = "0.1.0"
description = "Assignment 2: an MCP tool that calls a real external API"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=2.2.0",
]

[tool.uv]
package = false
