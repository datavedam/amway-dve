[project]
name = "assignment-06-oauth-login"
version = "0.1.0"
description = "Assignment 6: a self-contained OAuth 2.1 authorization server + MCP resource server, backed by SQLite"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=2.2.0",
]

[tool.uv]
package = false
