[project]
name = "assignment-05-jwt-login"
version = "0.1.0"
description = "Assignment 5: a local login server that issues JWTs, and an MCP server that verifies them"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=2.2.0",
    "pyjwt>=2.10.0",
]

[tool.uv]
package = false
